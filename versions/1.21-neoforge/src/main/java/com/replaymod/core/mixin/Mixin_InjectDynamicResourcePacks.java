package com.replaymod.core.mixin;

// NeoForge: Dynamic resource pack injection handled via AddPackFindersEvent in ReplayModBackend.
// This stub replaces the Fabric-only mixin so the compiler does not see an empty class file.
class Mixin_InjectDynamicResourcePacks {}
